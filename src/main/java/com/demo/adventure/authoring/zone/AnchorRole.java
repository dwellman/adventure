package com.demo.adventure.authoring.zone;

/**
 * Roles for anchor plots that shape pacing and spacing in a generated zone.
 */
public enum AnchorRole {
    ENTRY,
    EXIT,
    SET_PIECE,
    RESOURCE,
    GATEKEEPER,
    VISTA
}
